<h1>Enrolment Form</h1>

<%= render 'form' %>

<%= link_to 'Back | Pre Pre Training Review',  edit_pre_training_review_path(current_user.try(:pre_training_reviews).first), class: "btn btn-primary pull-left" %>